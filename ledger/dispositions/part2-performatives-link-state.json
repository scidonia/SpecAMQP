{
 "conventions": {
  "class": "What the coverage gate counts: MUST for both MUST and MUST NOT, else the clause's own keyword. Read from ledger/clauses.json rather than inferred, and carried on every entry beside `kind` so the two can be compared - they diverge for exactly the MUST NOT clauses, of which this file has seven.",
  "deferred:": "A real obligation this plan carries; the suffix names the milestone whose scope in PLAN.md the clause falls in - S4 for the session, link and flow-control layer. The note names the gap that keeps the clause out of the carrier set. That the milestone's slice has landed does not make the entry stale: what it records is that the obligation was left uncarried, which is the finding.",
  "formalized:": "A Lean declaration path that carries this clause's meaning. `check` resolves each value against the module tree and the declaration's own enclosing namespace, so a value names a declaration that exists; the note gives the file and line, and says which part of the declaration is the carrier when the declaration is larger than the rule.",
  "kind": "The clause's own keyword, read from the ledger's clause rather than inferred from the coverage class. The class is MUST for both forms, so a file whose entries all read MUST has lost the MUST NOT half of its own subject; the seven MUST NOT entries here are listed in design_consequences so the count is a claim rather than a count-up.",
  "superseded:": "The clause's open question is decided by an entry in ledger/ambiguities/, named by id: here, which condition a flow reports when it carries a link field with no handle, which the artifact leaves to the frame-body error family rather than stating.",
  "test:": "A vector that witnesses the clause's rule. `check` resolves the id against vectors/**/*.ndjson, and the note says what the vector's verdict would be if the rule were absent, so a `test:` value is a claim about a vector rather than a pointer to one.",
  "uncarried": "No declaration and no vector carries the clause's requirement. A declaration that only recognises the vocabulary a clause is written in - reading a field without acting on it, or recording a composite opaquely - is not a carrier; where one of those exists the note names it as the near-miss so a reader can see why it was not counted, and the near-miss is given with a file and a line so the note can be checked against the tree rather than against itself.",
  "formalized: names a declaration": "**A declaration, not a field.** A clause carried by *recording* a choice in a structure field is cited by the predicate that **reads** it, with the field named in the note: the check collects declarations, so a field is invisible to it, and the predicate is the part that enforces the sentence. `senderSettleMode`, `senderSettleUnsettled` and `receiverSettleSecond` are fields of `Session` for exactly this reason, cited as `negotiatedSettleRefusal`, `unsettledSettleRefusal` and `rcvSettleRefusal`."
 },
 "design_consequences": [
  "Seven of the twenty-five entries in this file are MUST NOT clauses and eighteen are MUST: `settled.1-.6`, `delivery-count.2-.3`, `incomplete-unsettled.2-.3`, `unsettled.1` and `.6`, `resume.1` and `.3`, `handle.2`, `disposition.2` and `.4`, and `next-incoming-id.1` read MUST; `delivery-count.1` and `.4`, `incomplete-unsettled.1`, `unsettled.5`, `resume.2`, `handle.1` and `next-incoming-id.2` read MUST NOT. The count is stated because the class is MUST for both forms and a file that marked them all MUST would be indistinguishable from a correct one.",
  "The batch's one behavioural finding is a settle-mode keying, and it is recorded on three refs rather than only as prose: `transfer/field:settled.4`, `.5` and `.6`. The artifact's two sentences are distinguished only by the `choice` attribute of their cross-references - `<xref name=\"sender-settle-mode\" choice=\"settled\"/>` at spec/oasis/amqp-core-transport-v1.0-os.xml:3153 for the clause requiring the flag true on at least one transfer, and `choice=\"unsettled\"` for the clause requiring it false or unset on every transfer (spec/oasis/amqp-core-transport-v1.0-os.xml:3153 and :3158) - and the ledger *used to* render both as the enclosing element's name (`\u00absender-settle-mode\u00bb`), so `.4` and `.6` presented identical antecedents with opposite obligations. **That rendering has since been corrected**: a cross-reference carrying a `choice` now renders the choice's own name (`\u00absettled\u00bb`, `\u00abunsettled\u00bb`) and records it in `references` as `sender-settle-mode/choice:settled`, so the two clauses are distinguishable in the text a reader sees. The finding below was made *because* they were indistinguishable, and it stands on its own \u2014 the collapse is what the ledger's own text invited, and the model, the reference and the corpus generator all read it that way \u2014 until the keying was corrected in all three at once and the two read as one condition. Both artefacts and the corpus then key the obligation on the `unsettled` value: Spec.Session.attachLink (lean/Spec/Session.lean:768-887), Ref.Session's counterpart (lean/Ref/Session.lean:556) and the generator's `sender_settle=0` (scripts/gen/slices.py:1452). Read either way, one of the two clauses is unenforced: as keyed, `.4` fires on `.6`'s value and `.6` is enforced backwards.",
  "Two of this file's carried clauses are carried in half, and the halves are worth naming because a reader who takes `formalized:` for full coverage would be wrong. `flow/field:delivery-count.2` requires a sender-issued flow to *set* the field to the current count; Spec.Session.flowCountRefusal? refuses a field that is present and wrong (lean/Spec/Session.lean:650-661) and admits a flow that leaves it out, so the value half is enforced and the presence half is not. The picture clauses have a comparable half-carry in `attach/field:handle.2`, where the refusal and the close live in two modules (lean/Spec/Session.lean:696-699 and lean/Spec/SessionCodec.lean:172-181) and neither alone is the obligation.",
  "`flow/field:delivery-count` splits in a way the three sentences do not suggest: `.2` is carried for both frame directions by one selector, and `.3` - the same field issued by the receiver - is not carried at all, because that selector asks which end issued the frame and returns `none` for the receiver's own flows (lean/Spec/Session.lean:646-647). `.4` is the sentence that a receiving endpoint which has not seen the sender's attach must not set the field, and the layer admits that frame: an outbound receiver has a handle and a position and no peer role, so linkHandleOf passes and flowCountRefusal? declines to look.",
  "`attach/field:handle.1`'s carrier is stricter than the clause and the difference is deliberate and recorded: the registry the guard reads is cumulative, so a handle released by a detach cannot be reused although it no longer names an open link (lean/Proofs/HandleUniqueness.lean:36-42). The direction of the strictness matters for what the corpus can witness - `exchange-link-handle-in-use` exercises reuse of a *live* handle, which is the clause's own case - and the note on the entry says so rather than leaving a reader to discover it.",
  "The widening moves the incomplete-map latch, null-key and re-attach checks, and all three transfer resume guards into `Spec.Protocol`, whose vectors pin the observable boundary. `attach/field:unsettled.1` remains deferred because its comparison is only a named state facet, not a step the endpoint consults. `disposition.4` remains deferred beyond the in-progress delivery case because the tag-keyed table is not enumerable by the delivery-id range a disposition carries.",
  "The restricted `Spec.Session` carriers cited by older entries remain valid only for the frozen single-link contracts that quantify over that model. New widening dispositions cite `Spec.Protocol` explicitly: the widened codecs never dispatch through or synchronize the restricted link fields."
 ],
 "dispositions": {
  "amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:attach/field:handle.1": {
   "class": "MUST",
   "clause_text": "The handle MUST NOT be used for other open links.",
   "disposition": "formalized:Spec.Session.attachLink; formalized:Proofs.HandleUniqueness.attachLink_handle_uniqueness; test:exchange-link-handle-in-use",
   "kind": "MUST NOT",
   "note": "Carried. attachLink refuses an attach whose handle is already in the registry it would add to (`refuseUnless (!claimed.contains handle)`, lean/Spec/Session.lean:696-699, over `claimed := if outbound then session.handles else session.peerHandles` at :669 and the two lists at :446 and :448). Proofs/HandleUniqueness.lean:161-186 is the invariant over the same guard, and it is the honest statement of what the layer enforces: the registry is cumulative rather than live (its module doc, lean/Proofs/HandleUniqueness.lean:36-42), so a handle released by detachLink (lean/Spec/Session.lean:1165-1188) may not be reused even though it no longer names an *open* link - stricter than this clause, in the direction a conformance claim can afford. The witness is `exchange-link-handle-in-use` in vectors/generated-exchanges.ndjson; vectors/isolation.md:99 records it as isolated, failing exactly that vector's step 4 when the guard is removed.",
   "text_sha256": "defbbe5cad710a62adddb3d03dc756677ff0dab7eebbcecd4e8c465bd0c4f69e"
  },
  "amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:attach/field:handle.2": {
   "class": "MUST",
   "clause_text": "An attempt to attach using a handle which is already associated with a link MUST be responded to with an immediate \u00abclose\u00bb carrying a handle-in-use \u00absession-error\u00bb.",
   "disposition": "formalized:Spec.Session.attachLink; formalized:Spec.SessionCodec.stepOf; test:exchange-link-handle-in-use",
   "kind": "MUST",
   "note": "Carried, by two layers because the session cannot write the close itself. The refusal is the same guard as `.1` raised with `closingRefusal handleInUse \"illegalState\"` (lean/Spec/Session.lean:696-699; the constructor at :206-207 sets `closesConnection := true`, and `handleInUse` at :223 reads the session-error choice table). The close is then performed by Spec.SessionCodec.stepOf, which moves the connection to CLOSE_SENT on a refusal carrying that flag (lean/Spec/SessionCodec.lean:172-181) - the immediate close this clause mandates. The witness is `exchange-link-handle-in-use` in vectors/generated-exchanges.ndjson, whose step 4 pins exactly that observable: the response reaches CLOSE_SENT on the connection rather than moving the session. vectors/isolation.md:17 and :99 cite that guard as Spec/Session.lean:621-624, a stale line; the live guard is :686-689.",
   "text_sha256": "adc9bb8ed018f5a867c8aafe231ca101fc75c0acc3a809e6ac17872f19371ab6"
  },
  "amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:attach/field:incomplete-unsettled.1": {
   "class": "MUST",
   "clause_text": "On receipt of an incomplete unsettled map a sending endpoint MUST NOT send any new deliveries (i.e. deliveries where resume is not set to true) to its partner (and a receiving endpoint which sent an incomplete unsettled map MUST detach with an error on receiving a transfer which does not have the resume flag set to true).",
   "disposition": "formalized:SpecAMQP.Spec.Protocol.incompleteFlag; formalized:SpecAMQP.Spec.Protocol.transferLink; test:staged-link-incomplete-map-latch; test:staged-link-incomplete-map-receipt",
   "kind": "MUST NOT",
   "note": "Carried by the endpoint-local incomplete-map latches and `transferLink`. `incompleteFlag` reads each attach's flag into the endpoint that announced it (lean/Spec/Protocol.lean:166-169, :469-480). A sender is refused when it starts a non-resumed delivery while the peer's latch is set, and a receiver is refused when a non-resumed transfer arrives after its own incomplete map (lean/Spec/Protocol.lean:691-707). The two staged vectors pin those directional halves and their illegal-state refusal.",
   "text_sha256": "9ff958e667700e5077708b6ce00356a90c6fc27c13dad6be9ce2ce914c336d71"
  },
  "amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:attach/field:incomplete-unsettled.2": {
   "class": "MUST",
   "clause_text": "On receipt of an incomplete unsettled map a sending endpoint MUST NOT send any new deliveries (i.e. deliveries where resume is not set to true) to its partner (and a receiving endpoint which sent an incomplete unsettled map MUST detach with an error on receiving a transfer which does not have the resume flag set to true).",
   "disposition": "formalized:SpecAMQP.Spec.Protocol.incompleteFlag; formalized:SpecAMQP.Spec.Protocol.transferLink; test:staged-link-incomplete-map-latch; test:staged-link-incomplete-map-receipt",
   "kind": "MUST",
   "note": "Carried by the same declarations and two directional scenarios as `.1`; the ledger keys one sentence twice with one digest, so both entries record the same decision. `transferLink` reads the local or remote endpoint latch according to frame direction (lean/Spec/Protocol.lean:691-707).",
   "text_sha256": "9ff958e667700e5077708b6ce00356a90c6fc27c13dad6be9ce2ce914c336d71"
  },
  "amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:attach/field:incomplete-unsettled.3": {
   "class": "MUST",
   "clause_text": "Note that if this flag is set to true then the endpoints MUST detach and reattach at least once in order to send new deliveries.",
   "disposition": "formalized:SpecAMQP.Spec.Protocol.incompleteFlag; formalized:SpecAMQP.Spec.Protocol.detachStep; formalized:SpecAMQP.Spec.Protocol.attachStep; test:staged-link-incomplete-map-latch",
   "kind": "MUST",
   "note": "Carried through the latch's observable lift. The stable link identity survives `detachStep`; a later complete attach writes `incompleteFlag = false` for that same endpoint (lean/Spec/Protocol.lean:166-169, :469-480, :559-582). `staged-link-incomplete-map-latch` first observes a new delivery refused while latched, then admitted only after detach and complete re-attach, pinning the required at-least-once cycle by its consequence.",
   "text_sha256": "e9f22c086be29bd22af6ca09b1be411f8fd2407f29f3f1da04a30a7ed898df43"
  },
  "amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:attach/field:unsettled.1": {
   "class": "MUST",
   "clause_text": "The local and remote delivery states for a given delivery-tag MUST be compared to resolve any in-doubt deliveries.",
   "disposition": "deferred:S4",
   "kind": "MUST",
   "note": "Still deferred as an endpoint obligation, with its state facet now explicit. `Spec.Protocol.resolve` compares the local and remote states stored together at one delivery tag; `SpecAMQP.Proofs.resolve_absent`, `resolve_agrees`, and `resolve_disputed` prove it reports absence, agreement, and disagreement rather than choosing silently (lean/Proofs/WidenedClauses.lean:36-69). No executable step consults that comparison, so those theorems are not counted as proof that the endpoint performed the clause's required resolution.",
   "text_sha256": "2b7a4bc1f95da419e0fb30bc618392c308baa852ade48e7597a6d9e5d5401167"
  },
  "amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:attach/field:unsettled.5": {
   "class": "MUST",
   "clause_text": "The unsettled map MUST NOT contain null valued keys.",
   "disposition": "formalized:SpecAMQP.Spec.Protocol.unsettledKeyList; formalized:SpecAMQP.Spec.Protocol.unsettledKeys; test:staged-link-attach-unsettled-null-key",
   "kind": "MUST NOT",
   "note": "Carried by the widened unsettled-map decoder. `unsettledKeyList` rejects a null map key as `invalid-field`/`malformed`, while retaining binary delivery tags (lean/Spec/Protocol.lean:144-164); `attachStep` must pass that decoder before changing the registry. `staged-link-attach-unsettled-null-key` pins the refusal.",
   "text_sha256": "80ac6c67714c081d20875715f9fed2a6aa6603f227f0fd92517c9809ed731a79"
  },
  "amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:attach/field:unsettled.6": {
   "class": "MUST",
   "clause_text": "When reattaching (as opposed to resuming), the unsettled map MUST be null.",
   "disposition": "formalized:SpecAMQP.Spec.Protocol.unsettledKeys; formalized:SpecAMQP.Spec.Protocol.attachStep; test:staged-link-reattach-with-unsettled-map",
   "kind": "MUST",
   "note": "Carried by stable link life plus unsettled-field presence. `unsettledKeys` distinguishes null/absent from a map, and `attachStep` refuses a present map when the named endpoint is detached, while reserving a present map for resume of a destroyed endpoint (lean/Spec/Protocol.lean:160-164, :419-440). `staged-link-reattach-with-unsettled-map` pins the ordinary re-attach refusal.",
   "text_sha256": "311267e76647ce03c9a5c4b8ad5ec547062adfe1e400a9b6dda4f3ae5e209cb0"
  },
  "amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:disposition.2": {
   "class": "MUST",
   "clause_text": "The disposition frame MAY reference deliveries from many different links associated with a session, although all links MUST have the directionality indicated by the specified role.",
   "disposition": "formalized:Spec.Session.dispositionLink; formalized:SpecAMQP.Contracts.DispositionsInTheOtherDirectionAreRefused; test:exchange-link-disposition-direction",
   "kind": "MUST",
   "note": "Carried. dispositionLink refuses a disposition whose role is not the role this endpoint attached as (lean/Spec/Session.lean:996-999), which is this sentence's MUST half: the links named must have the directionality the role indicates, so one naming the other end's role is refused with amqp:invalid-field. Contracts/Settlement.lean:103-109 states the same obligation over that constructor. The witness is `exchange-link-disposition-direction` in vectors/generated-exchanges.ndjson: the same frame with the endpoint's own role is admitted and the other role refused in the next step. Its citation array names the *same sentence's* other index - `amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:disposition.1`, the MAY half - so the corpus cites the sentence once where the ledger keys it twice.",
   "text_sha256": "41c130754fae056386e9520ffbe587a1357e29992f2c16cbb2c5419102811227"
  },
  "amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:disposition.4": {
   "class": "MUST",
   "clause_text": "As long as the links have not been closed or detached with an error then the deliveries are still \"live\" and the updated state MUST be applied.",
   "disposition": "deferred:S4",
   "kind": "MUST",
   "note": "Partially carried and still deferred at the table boundary. `detachStep` preserves the stable link and its unsettled history on a detach without error; `dispositionStep` traverses every matching-role registered link, and `applyDisposition` settles the in-progress delivery whose id is in range (lean/Spec/Protocol.lean:559-637). `staged-disposition-after-detach` pins that case. But a completed unsettled delivery remains only in the tag-keyed table, and the handle-less disposition supplies ids rather than tags; the current model cannot enumerate those table entries, as `applyDisposition`'s own doc records. The declaration therefore does not carry the clause for every live delivery and is not promoted.",
   "text_sha256": "d7e9fdf7d4b348d878395fda351544d5126301cd8c98f803a0e62e8493354339"
  },
  "amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:flow/field:delivery-count.1": {
   "class": "MUST",
   "clause_text": "When the handle field is not set, this field MUST NOT be set.",
   "disposition": "formalized:Spec.Session.flowLink; superseded:flow-link-field-without-handle; test:exchange-link-flow-field-without-handle",
   "kind": "MUST NOT",
   "note": "Carried. Spec.Session.flowLink refuses a flow whose handle is unset and which carries any of the five link fields - this one among them - by collecting the fields the frame sets and requiring the list to be empty (lean/Spec/Session.lean:763-775). The condition it reports is decided rather than quoted: superseded:flow-link-field-without-handle adopts amqp:invalid-field for exactly this case, and the ledger has no clause for the condition itself. The witness is `exchange-link-flow-field-without-handle` in vectors/generated-exchanges.ndjson, whose own citation array names this ref along with the four sibling fields; its note records why it carries `available` rather than one of the fields a later rule reads.",
   "text_sha256": "29f0e6894addb861e1ea4b484a300718cbe2908d81bdb8c3f1ed10e574d8c61c"
  },
  "amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:flow/field:delivery-count.2": {
   "class": "MUST",
   "clause_text": "When the handle identifies that the flow state is being sent from the sender link endpoint to receiver link endpoint this field MUST be set to the current delivery-count of the link endpoint.",
   "disposition": "formalized:Spec.Session.flowCountRefusal; formalized:Spec.Session.flowLink",
   "kind": "MUST",
   "note": "Carried in its value half. Spec.Session.flowCountRefusal? tests a flow's delivery-count against `position.deliveryCount` whenever the frame's issuer is the link's sender: `ours := if outbound then role == sender else role == receiver` (lean/Spec/Session.lean:646-647), so the rule fires on an outbound flow from a sender and on an inbound flow to a receiver, which are this clause's two frame directions, and it refuses a value that differs from the count the endpoint holds. The presence half is not enforced, and the gap is in the same three lines: the guard is a match on `fieldValue \"flow\" \"delivery-count\" body` (:645-649), so a flow that omits the field falls to the catch-all and is admitted although this clause says it MUST be set. The condition is decided rather than quoted: superseded:attach-retains-link-flow-state, whose own clause list names this ref, adopts the reading that a later attach cannot replace the flow state the link has already agreed. Contracts/SessionCredit.lean:44-48 records this guard and `.3` as conditions inside the step function rather than state laws, and states no law over them. The witness is `exchange-link-attach-keeps-the-credit` in vectors/generated-exchanges.ndjson, whose citation array names this ref: the peer is the link's sender, its flow must report the count this endpoint's view holds, and the transfer that follows is admitted on credit the link retained. **The presence half is named in the value rather than only here**, because a `formalized:` that covers half a sentence conflates completed work with pending work: the clause's two obligations are its value and its being set at all, flowCountRefusal carries the first, and nothing carries the second. `vectors/generated-exchanges.ndjson` is the evidence record; no separate staged corpus remains after the widening promotion. **Carried this session, and the reporter's own words for it**: formalized:Spec.Session.flowCountRefusal (+ Spec.Session.flowLink). Both halves are now read: the presence half (a flow naming the link must set the field) was the half the old carrier missed, and the value half is unchanged in meaning but no longer filtered to two of the four directions.",
   "text_sha256": "8cec0d9917be03fd75b65e1f3c355afcb3867416680ab7e0627ccb0869e65bf5"
  },
  "amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:flow/field:delivery-count.3": {
   "class": "MUST",
   "clause_text": "When the flow state is being sent from the receiver endpoint to the sender endpoint this field MUST be set to the last known value of the corresponding sending endpoint.",
   "disposition": "formalized:Spec.Session.flowCountRefusal",
   "kind": "MUST",
   "note": "Uncarried, and the near-miss is the same declaration delivery-count.2 is carried by, read for what it excludes: Spec.Session.flowCountRefusal?'s selector (lean/Spec/Session.lean:646-647) is true only when the frame's issuer is the link's *sender*, and returns `none` otherwise (`if !ours then none`), so a flow issued by the receiver - the case this sentence is written for, in either frame direction - is never compared with the last known value of the sending endpoint. Contracts/SessionCredit.lean:44-48 names this clause as one of the three guards \"inside the step function\", and glosses it as \"a flow received carries the count this endpoint's view holds\", which is `.2`'s case: a received flow whose issuer is the sender. The gloss is the reading the layer implements, and it is not this sentence. No vector's citation array names this ref. **Carried this session, and the reporter's own words for it**: formalized:Spec.Session.flowCountRefusal. The old selector asked which end *issued* the frame and returned none for a receiver's flow; the unified reading is that position.deliveryCount *is* the last known value of the sending endpoint in every direction, so a receiver's flow is compared like a sender's. Promoted vector `exchange-flow-receiver-count-not-echoed` in vectors/generated-exchanges.ndjson now refuses.",
   "text_sha256": "1239beb0e052b9fc754526fc7afadb842f19f5896795142d26029f15b3b41b84"
  },
  "amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:flow/field:delivery-count.4": {
   "class": "MUST",
   "clause_text": "In the event that the receiving link endpoint has not yet seen the initial \u00abattach\u00bb frame from the sender this field MUST NOT be set.",
   "disposition": "formalized:Spec.Session.flowCountRefusal",
   "kind": "MUST NOT",
   "note": "Uncarried, and reachable rather than vacuous. The clause is the second sentence of `.3`'s paragraph: a receiving link endpoint that has not yet seen the sender's attach MUST NOT set this field. The layer admits that frame. An endpoint that attaches outbound as the link's receiver has `handle` and `position` set while `peerHandle` and `peerRole` are none (lean/Spec/Session.lean:721-724), and a flow it sends naming its own handle passes linkHandleOf (lean/Spec/Session.lean:645-684) and reaches flowCountRefusal? with `ours` false (lean/Spec/Session.lean:646-647), so the field is unchecked and the flow is admitted. The near-miss read and rejected is that selector: it asks whether the issuer is the sender, and this clause asks whether the issuer has seen the sender's attach, which the model holds no flag for. The second near-miss is linkHandleOf's unattached-handle refusal (lean/Spec/Session.lean:621-624), which covers a *received* frame naming an unknown handle and so would refuse this case only if the endpoint were the sender, not the receiver. **Carried this session, and the reporter's own words for it**: formalized:Spec.Session.flowCountRefusal (new half). The receiving link endpoint that has not yet seen the sender's attach must NOT set the field; reachable because a peer may attach as receiver and send a flow before this endpoint's own attach, and the probe vector exercises both the refusal and the conforming absence.",
   "text_sha256": "ff731fda5a263c4dde40fcb0c8adc38b13b4e7adf04be036caeea81a73b2d7e8"
  },
  "amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:flow/field:next-incoming-id.1": {
   "class": "MUST",
   "clause_text": "This value MUST be set if the peer has received the \u00abbegin\u00bb frame for the session, and MUST NOT be set if it has not.",
   "disposition": "formalized:Spec.Session.step; test:exchange-session-flow-missing-next-incoming-id; test:exchange-session-incoming-id-must-be-set",
   "kind": "MUST",
   "note": "Carried. Spec.Session.step's flow arm holds the frame's setness to the session's own flag: `refuseUnless (set == session.peerBegun)` (lean/Spec/Session.lean:1270), where peerBegun is \"whether the partner's begin has arrived\" (:429-430), set by a received begin (:1119, :1126) and cleared by an end (:1143, :1155). Two witnesses, both in vectors/generated-exchanges.ndjson and both citing this ref: `exchange-session-flow-missing-next-incoming-id` sends a flow with the field unset after the begin has arrived and is refused, and `exchange-session-incoming-id-must-be-set` sends one with it set and is admitted. The reading the layer takes is worth naming: the artifact says \"if the peer has received the begin frame\", and the model reads it as the partner's begin having arrived *here*, which is the state the session can see.",
   "text_sha256": "bcf10ca56764ca96afeb75cbf621209606979e6347dfa667d1b72d5849fd3a3d"
  },
  "amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:flow/field:next-incoming-id.2": {
   "class": "MUST",
   "clause_text": "This value MUST be set if the peer has received the \u00abbegin\u00bb frame for the session, and MUST NOT be set if it has not.",
   "disposition": "formalized:Spec.Session.step",
   "kind": "MUST NOT",
   "note": "Carried by the same equality as `.1` - `set == session.peerBegun` (lean/Spec/Session.lean:1270) is one rule with two halves, and its false side is this MUST NOT: a flow sent before the partner's begin has arrived must leave the field unset. The ledger keys the sentence twice with one digest (bcf10ca5...), so the two entries decide the same words. The corpus witnesses the other half: both vectors that cite this ref (`exchange-session-flow-missing-next-incoming-id`, `exchange-session-incoming-id-must-be-set` in vectors/generated-exchanges.ndjson) send their flow after the begin has arrived, so the unset-before-begin case is carried by the declaration and unwitnessed by a vector.",
   "text_sha256": "bcf10ca56764ca96afeb75cbf621209606979e6347dfa667d1b72d5849fd3a3d"
  },
  "amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:transfer/field:resume.1": {
   "class": "MUST",
   "clause_text": "The receiver MUST ignore resumed deliveries that are not in its local unsettled map.",
   "disposition": "formalized:SpecAMQP.Spec.Protocol.resumable; formalized:SpecAMQP.Spec.Protocol.transferLink; test:staged-link-resume-not-in-receiver-map; test:staged-link-incomplete-map-receipt",
   "kind": "MUST",
   "note": "Carried by unsettled-table membership at `transferLink`. When a received transfer sets `resume` but its tag is absent, `resumable` is false and the function returns the original session, admitting the frame while recording none of its delivery (lean/Spec/Protocol.lean:173-178, :691-701). `staged-link-resume-not-in-receiver-map` pins the ignore rule directly; the incomplete-map scenario pins the competing non-resumed refusal.",
   "text_sha256": "80c6e25549dc9a573b1f4b807aff0e289b1298708f1886dca7749f89649ff07d"
  },
  "amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:transfer/field:resume.2": {
   "class": "MUST",
   "clause_text": "The sender MUST NOT send resumed transfers for deliveries not in its local unsettled map.",
   "disposition": "formalized:SpecAMQP.Spec.Protocol.resumable; formalized:SpecAMQP.Spec.Protocol.transferLink; test:staged-link-resume-sent-not-in-map",
   "kind": "MUST NOT",
   "note": "Carried by the sending half of the same membership check. A sent transfer marked `resume` whose tag is absent from the logical link's shared unsettled table is refused as `invalid-field`/`malformed` (lean/Spec/Protocol.lean:691-701). `staged-link-resume-sent-not-in-map` pins that sender-side MUST NOT.",
   "text_sha256": "f275f2cf16b5442a4dfeaf388b4c0898153329b70d976d79180cc496df3fb241"
  },
  "amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:transfer/field:resume.3": {
   "class": "MUST",
   "clause_text": "If a resumed delivery spans more than one transfer performative, then the resume flag MUST be set to true on the first transfer of the resumed delivery.",
   "disposition": "formalized:SpecAMQP.Spec.Protocol.deliveryStep; formalized:SpecAMQP.Spec.Protocol.transferLink; test:staged-link-resume-only-on-continuation",
   "kind": "MUST",
   "note": "Carried by the in-progress delivery's first-transfer claim. `deliveryStep` records `resumed` only when a delivery begins and preserves it on continuations; `transferLink` refuses a continuation that sets resume when the held delivery did not begin resumed (lean/Spec/Protocol.lean:180-187, :691-701). `staged-link-resume-only-on-continuation` pins the `invalid-field`/`malformed` refusal, paired with the admitted first-transfer-resume scenario.",
   "text_sha256": "cd44d704f4762c3b828249d570fdaf0d42f32a9b72bc2ef9dc19305633be5c2c"
  },
  "amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:transfer/field:settled.1": {
   "class": "MUST",
   "clause_text": "If not set on the first (or only) transfer for a (multi-transfer) delivery, then the settled flag MUST be interpreted as being false.",
   "disposition": "formalized:Spec.Session.Delivery.step; formalized:SpecAMQP.Contracts.unsettled_first_transfer_stays_unsettled",
   "kind": "MUST",
   "note": "Carried. Delivery.step (lean/Spec/Session.lean:410-414) is the interpretation the clause names: its `none` branch - the first (or only) transfer of a delivery - records the frame's own flag rather than a default, so an unset flag is false and nothing invents a value where the field was silent. Contracts/Settlement.lean:82-83 states that half as an equation, `(Delivery.step none id false).settled = false`, closed by rfl against that branch. The declaration the docstring sits on repeats the clause in that docstring (lean/Spec/Session.lean:403-406, `structure Delivery`); no vector's citation array names this ref, and the corpus's first-transfer vectors exercise the neighbouring clauses instead.",
   "text_sha256": "a9247e7e0c41e82fb7f22f8d55075f01abad22c6f50ba9b5e654acd868b5f1d5"
  },
  "amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:transfer/field:settled.2": {
   "class": "MUST",
   "clause_text": "For subsequent transfers in a multi-transfer delivery if the settled flag is left unset then it MUST be interpreted as true if and only if the value of the settled flag on any of the preceding transfers was true; if no preceding transfer was sent with settled being true then the value when unset MUST be taken as false.",
   "disposition": "formalized:Spec.Session.Delivery.step; formalized:SpecAMQP.Contracts.delivery_settlement_is_monotone; test:exchange-link-settled-inherited",
   "kind": "MUST",
   "note": "Carried. Delivery.step's `some delivery` branch is `delivery.settled || settled` (lean/Spec/Session.lean:413), which is this sentence's rule in full: a continuation whose flag is unset reports true exactly when a preceding transfer of that delivery set it true, and false when none did. Contracts/Settlement.lean:71-74 states the monotone half as a theorem over the same constructor. The witness is `exchange-link-settled-inherited` in vectors/generated-exchanges.ndjson: a two-transfer delivery whose continuation leaves the flag unset is admitted, and what distinguishes it from every other transfer vector is that its last frame does not carry the flag the delivery is settled by. Its own citation array names `amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:transfer/field:settled.4` rather than this sentence, which is one ref along.",
   "text_sha256": "0598931a326451d80e9fbfb3a1480ad290fa9299b707c22cff4a7df131dfc3cd"
  },
  "amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:transfer/field:settled.3": {
   "class": "MUST",
   "clause_text": "For subsequent transfers in a multi-transfer delivery if the settled flag is left unset then it MUST be interpreted as true if and only if the value of the settled flag on any of the preceding transfers was true; if no preceding transfer was sent with settled being true then the value when unset MUST be taken as false.",
   "disposition": "formalized:Spec.Session.Delivery.step; formalized:SpecAMQP.Contracts.delivery_settlement_is_monotone; test:exchange-link-settled-inherited",
   "kind": "MUST",
   "note": "Carried, by the same two declarations as `.2`. The ledger keys this sentence twice - `.2` and this ref carry the same text and the same digest (0598931a...) - so the two entries decide the same words, which is what the disposition mechanism requires of a duplicated statement; a disposition re-keyed onto one of them alone would leave the other reading as undecided. The note is on `.2` and this entry repeats the decision rather than the argument.",
   "text_sha256": "0598931a326451d80e9fbfb3a1480ad290fa9299b707c22cff4a7df131dfc3cd"
  },
  "amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:transfer/field:settled.4": {
   "class": "MUST",
   "clause_text": "If the negotiated value for snd-settle-mode at attachment is \u00absettled\u00bb, then this field MUST be true on at least one transfer frame for a delivery (i.e., the delivery MUST be settled at the sender at the point the delivery has been completely transferred).",
   "disposition": "formalized:Spec.Session.transferLink; formalized:Proofs.Settlement.senderSettleModeIsTheChoiceTheClauseSelects",
   "kind": "MUST",
   "note": "Uncarried, and the near-miss is exact: Spec.Session.attachLink computes the link's settle mode as `valueNat value == some unsettled` against the choice table's `unsettled` value (lean/Spec/Session.lean:708-720, the number read from `choiceValue? \"sender-settle-mode\" \"unsettled\"`), and Spec.Session.transferLink then refuses a delivery not settled in any of its transfers when that flag is set (lean/Spec/Session.lean:896-899). This clause is conditioned on the other value: the artifact's `<xref name=\"sender-settle-mode\" choice=\"settled\"/>` at spec/oasis/amqp-core-transport-v1.0-os.xml:3153, whose `choice` attribute is what selects this clause: the ledger rendered it as the element's name and so lost the choice, until that rendering was corrected to name the choice. Read the choice attribute and `.4` turns on `snd-settle-mode = settled` while `.6` turns on `unsettled`, which is the only reading under which the two sentences are not contradictory; the model's guard therefore has this clause's shape keyed on the value `.6` names. Contracts/Settlement.lean:141-158 states the obligation over the same flag (`SenderSettleModeDeliveriesAreSettledOnATransfer`, stated and not proved) and so inherits the keying rather than repairing it. The corpus agrees with the model rather than with the artifact: `exchange-link-sender-settle-mode-unmet` is generated with `snd_settle_mode=choice_value(TRANSPORT, \"sender-settle-mode\", \"settled\")` (`scripts/gen/slices.py:1465-1466`), the value this clause is conditioned on, and its step note names that choice. It is still not recorded as a `test:` witness here, and that is now a decision rather than an omission: the vector's verdict is the refusal this clause obliges, which makes it a candidate witness rather than a counterexample.",
   "text_sha256": "1e792e6e05999409e27149c7e496cc61e6b96f6841a248732d64cfb7125fc1f5"
  },
  "amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:transfer/field:settled.5": {
   "class": "MUST",
   "clause_text": "If the negotiated value for snd-settle-mode at attachment is \u00absettled\u00bb, then this field MUST be true on at least one transfer frame for a delivery (i.e., the delivery MUST be settled at the sender at the point the delivery has been completely transferred).",
   "disposition": "formalized:Spec.Session.transferLink; formalized:Proofs.Settlement.senderSettleModeIsTheChoiceTheClauseSelects",
   "kind": "MUST",
   "note": "Carried, by the same declaration as `.4`: the ledger keys this sentence twice with one digest \u2014 the two refs carry the same words \u2014 so this entry repeats the decision rather than the argument. The artifact distinguishes the pair only by the `choice` attribute of their cross-references, which is what the selection now reads.",
   "text_sha256": "1e792e6e05999409e27149c7e496cc61e6b96f6841a248732d64cfb7125fc1f5"
  },
  "amqp-core-transport-v1.0-os.xml#amqp:transport/section:performatives/type:transfer/field:settled.6": {
   "class": "MUST",
   "clause_text": "If the negotiated value for snd-settle-mode at attachment is \u00abunsettled\u00bb, then this field MUST be false (or unset) on every transfer frame for a delivery (unless the delivery is aborted).",
   "disposition": "formalized:Spec.Session.unsettledSettleRefusal; formalized:Spec.Session.negotiatedSettleRefusal",
   "kind": "MUST",
   "note": "Uncarried, and the model does more than omit it: this clause is conditioned on the artifact's `<xref name=\"sender-settle-mode\" choice=\"unsettled\"/>` at spec/oasis/amqp-core-transport-v1.0-os.xml:3158 and requires the flag false (or unset) on every transfer of a delivery unless the delivery is aborted, while Spec.Session.transferLink refuses a delivery that is *not* settled when the negotiated mode is that same value (lean/Spec/Session.lean:708-720 for the mode, :878-883 for the guard). So for the value this clause names the layer enforces the opposite of its sentence, and a peer that follows the artifact - sending settled unset throughout, as the unsettled choice's own doc describes (\"The sender will send all deliveries initially unsettled to the receiver\", spec/oasis/amqp-core-transport-v1.0-os.xml:3445, the choice's own doc) - is refused. The near-miss read and rejected is that same guard, which is settled.4's shape and not this clause's. Contrast `aborted`, which the clause excepts and `transferLink` does read (lean/Spec/Session.lean:856-857). **Carried this session, and the reporter's own words for it**: formalized:Spec.Session.unsettledSettleRefusal (+ Spec.Session.negotiatedSettleRefusal, applied at Spec.Session.step; the negotiation is recorded by the new Session.senderSettleUnsettled). Read as the unsettled *choice*, with the aborted exemption; enforced in both directions, as `exchange-link-settled-over-unsettled-negotiation` and `exchange-link-settled-over-unsettled-negotiation-sent` in vectors/generated-exchanges.ndjson require. The negotiation it reads lives in the field `Session.senderSettleUnsettled`; a field is invisible to the ledger's check, so the entry names the present predicate Spec.Session.unsettledSettleRefusal? at lean/Spec/Session.lean:980-984 that enforces the sentence.",
   "text_sha256": "a238ee047d7824eb826403c5a812bb23c5c693b57dd8be1457c780b09f4f8d8b"
  }
 }
}
