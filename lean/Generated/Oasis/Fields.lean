-- Generated by `scripts/gen-oasis-lean.py` from the pinned OASIS artifacts.
-- Do not edit: regenerate instead. The artifact byte identities this file was
-- derived from are recorded in `toolchain/sources.toml`, and the provenance
-- summary is `lean/Generated/Oasis/provenance.json`.

namespace SpecAMQP.Generated.Oasis

/-- A field of a composite type, in wire order. `defaultValue` records the
default the specification declares, which is part of the type's behaviour rather
than an implementation choice. -/
structure FieldDecl where
  owner : String
  ownerPath : String
  index : Nat
  name : String
  typeName : String
  mandatory : Bool
  defaultValue : Option String
  multiple : Bool
  requires : Option String
  label : String
  artifact : String
deriving Repr, DecidableEq

def fields : List FieldDecl := [
  { owner := "source", ownerPath := "amqp:messaging/section:addressing/type:source", index := 1, name := "address", typeName := "*", mandatory := false, defaultValue := none, multiple := false, requires := some "address", label := "the address of the source", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "source", ownerPath := "amqp:messaging/section:addressing/type:source", index := 2, name := "durable", typeName := "terminus-durability", mandatory := false, defaultValue := some "none", multiple := false, requires := none, label := "indicates the durability of the terminus", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "source", ownerPath := "amqp:messaging/section:addressing/type:source", index := 3, name := "expiry-policy", typeName := "terminus-expiry-policy", mandatory := false, defaultValue := some "session-end", multiple := false, requires := none, label := "the expiry policy of the source", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "source", ownerPath := "amqp:messaging/section:addressing/type:source", index := 4, name := "timeout", typeName := "seconds", mandatory := false, defaultValue := some "0", multiple := false, requires := none, label := "duration that an expiring source will be retained", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "source", ownerPath := "amqp:messaging/section:addressing/type:source", index := 5, name := "dynamic", typeName := "boolean", mandatory := false, defaultValue := some "false", multiple := false, requires := none, label := "request dynamic creation of a remote node", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "source", ownerPath := "amqp:messaging/section:addressing/type:source", index := 6, name := "dynamic-node-properties", typeName := "node-properties", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "properties of the dynamically created node", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "source", ownerPath := "amqp:messaging/section:addressing/type:source", index := 7, name := "distribution-mode", typeName := "symbol", mandatory := false, defaultValue := none, multiple := false, requires := some "distribution-mode", label := "the distribution mode of the link", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "source", ownerPath := "amqp:messaging/section:addressing/type:source", index := 8, name := "filter", typeName := "filter-set", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "a set of predicates to filter the messages admitted onto the link", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "source", ownerPath := "amqp:messaging/section:addressing/type:source", index := 9, name := "default-outcome", typeName := "*", mandatory := false, defaultValue := none, multiple := false, requires := some "outcome", label := "default outcome for unsettled transfers", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "source", ownerPath := "amqp:messaging/section:addressing/type:source", index := 10, name := "outcomes", typeName := "symbol", mandatory := false, defaultValue := none, multiple := true, requires := none, label := "descriptors for the outcomes that can be chosen on this link", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "source", ownerPath := "amqp:messaging/section:addressing/type:source", index := 11, name := "capabilities", typeName := "symbol", mandatory := false, defaultValue := none, multiple := true, requires := none, label := "the extension capabilities the sender supports/desires", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "target", ownerPath := "amqp:messaging/section:addressing/type:target", index := 1, name := "address", typeName := "*", mandatory := false, defaultValue := none, multiple := false, requires := some "address", label := "The address of the target.", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "target", ownerPath := "amqp:messaging/section:addressing/type:target", index := 2, name := "durable", typeName := "terminus-durability", mandatory := false, defaultValue := some "none", multiple := false, requires := none, label := "indicates the durability of the terminus", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "target", ownerPath := "amqp:messaging/section:addressing/type:target", index := 3, name := "expiry-policy", typeName := "terminus-expiry-policy", mandatory := false, defaultValue := some "session-end", multiple := false, requires := none, label := "the expiry policy of the target", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "target", ownerPath := "amqp:messaging/section:addressing/type:target", index := 4, name := "timeout", typeName := "seconds", mandatory := false, defaultValue := some "0", multiple := false, requires := none, label := "duration that an expiring target will be retained", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "target", ownerPath := "amqp:messaging/section:addressing/type:target", index := 5, name := "dynamic", typeName := "boolean", mandatory := false, defaultValue := some "false", multiple := false, requires := none, label := "request dynamic creation of a remote node", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "target", ownerPath := "amqp:messaging/section:addressing/type:target", index := 6, name := "dynamic-node-properties", typeName := "node-properties", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "properties of the dynamically created node", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "target", ownerPath := "amqp:messaging/section:addressing/type:target", index := 7, name := "capabilities", typeName := "symbol", mandatory := false, defaultValue := none, multiple := true, requires := none, label := "the extension capabilities the sender supports/desires", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "modified", ownerPath := "amqp:messaging/section:delivery-state/type:modified", index := 1, name := "delivery-failed", typeName := "boolean", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "count the transfer as an unsuccessful delivery attempt", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "modified", ownerPath := "amqp:messaging/section:delivery-state/type:modified", index := 2, name := "undeliverable-here", typeName := "boolean", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "prevent redelivery", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "modified", ownerPath := "amqp:messaging/section:delivery-state/type:modified", index := 3, name := "message-annotations", typeName := "fields", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "message attributes", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "received", ownerPath := "amqp:messaging/section:delivery-state/type:received", index := 1, name := "section-number", typeName := "uint", mandatory := true, defaultValue := none, multiple := false, requires := none, label := "", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "received", ownerPath := "amqp:messaging/section:delivery-state/type:received", index := 2, name := "section-offset", typeName := "ulong", mandatory := true, defaultValue := none, multiple := false, requires := none, label := "", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "rejected", ownerPath := "amqp:messaging/section:delivery-state/type:rejected", index := 1, name := "error", typeName := "error", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "error that caused the message to be rejected", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "header", ownerPath := "amqp:messaging/section:message-format/type:header", index := 1, name := "durable", typeName := "boolean", mandatory := false, defaultValue := some "false", multiple := false, requires := none, label := "specify durability requirements", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "header", ownerPath := "amqp:messaging/section:message-format/type:header", index := 2, name := "priority", typeName := "ubyte", mandatory := false, defaultValue := some "4", multiple := false, requires := none, label := "relative message priority", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "header", ownerPath := "amqp:messaging/section:message-format/type:header", index := 3, name := "ttl", typeName := "milliseconds", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "time to live in ms", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "header", ownerPath := "amqp:messaging/section:message-format/type:header", index := 4, name := "first-acquirer", typeName := "boolean", mandatory := false, defaultValue := some "false", multiple := false, requires := none, label := "", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "header", ownerPath := "amqp:messaging/section:message-format/type:header", index := 5, name := "delivery-count", typeName := "uint", mandatory := false, defaultValue := some "0", multiple := false, requires := none, label := "the number of prior unsuccessful delivery attempts", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "properties", ownerPath := "amqp:messaging/section:message-format/type:properties", index := 1, name := "message-id", typeName := "*", mandatory := false, defaultValue := none, multiple := false, requires := some "message-id", label := "application message identifier", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "properties", ownerPath := "amqp:messaging/section:message-format/type:properties", index := 2, name := "user-id", typeName := "binary", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "creating user id", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "properties", ownerPath := "amqp:messaging/section:message-format/type:properties", index := 3, name := "to", typeName := "*", mandatory := false, defaultValue := none, multiple := false, requires := some "address", label := "the address of the node the message is destined for", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "properties", ownerPath := "amqp:messaging/section:message-format/type:properties", index := 4, name := "subject", typeName := "string", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "the subject of the message", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "properties", ownerPath := "amqp:messaging/section:message-format/type:properties", index := 5, name := "reply-to", typeName := "*", mandatory := false, defaultValue := none, multiple := false, requires := some "address", label := "the node to send replies to", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "properties", ownerPath := "amqp:messaging/section:message-format/type:properties", index := 6, name := "correlation-id", typeName := "*", mandatory := false, defaultValue := none, multiple := false, requires := some "message-id", label := "application correlation identifier", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "properties", ownerPath := "amqp:messaging/section:message-format/type:properties", index := 7, name := "content-type", typeName := "symbol", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "MIME content type", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "properties", ownerPath := "amqp:messaging/section:message-format/type:properties", index := 8, name := "content-encoding", typeName := "symbol", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "MIME content type", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "properties", ownerPath := "amqp:messaging/section:message-format/type:properties", index := 9, name := "absolute-expiry-time", typeName := "timestamp", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "the time when this message is considered expired", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "properties", ownerPath := "amqp:messaging/section:message-format/type:properties", index := 10, name := "creation-time", typeName := "timestamp", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "the time when this message was created", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "properties", ownerPath := "amqp:messaging/section:message-format/type:properties", index := 11, name := "group-id", typeName := "string", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "the group this message belongs to", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "properties", ownerPath := "amqp:messaging/section:message-format/type:properties", index := 12, name := "group-sequence", typeName := "sequence-no", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "the sequence-no of this message within its group", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "properties", ownerPath := "amqp:messaging/section:message-format/type:properties", index := 13, name := "reply-to-group-id", typeName := "string", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "the group the reply message belongs to", artifact := "amqp-core-messaging-v1.0-os.xml" },
  { owner := "sasl-challenge", ownerPath := "amqp:security/section:sasl/type:sasl-challenge", index := 1, name := "challenge", typeName := "binary", mandatory := true, defaultValue := none, multiple := false, requires := none, label := "security challenge data", artifact := "amqp-core-security-v1.0-os.xml" },
  { owner := "sasl-init", ownerPath := "amqp:security/section:sasl/type:sasl-init", index := 1, name := "mechanism", typeName := "symbol", mandatory := true, defaultValue := none, multiple := false, requires := none, label := "selected security mechanism", artifact := "amqp-core-security-v1.0-os.xml" },
  { owner := "sasl-init", ownerPath := "amqp:security/section:sasl/type:sasl-init", index := 2, name := "initial-response", typeName := "binary", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "security response data", artifact := "amqp-core-security-v1.0-os.xml" },
  { owner := "sasl-init", ownerPath := "amqp:security/section:sasl/type:sasl-init", index := 3, name := "hostname", typeName := "string", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "the name of the target host", artifact := "amqp-core-security-v1.0-os.xml" },
  { owner := "sasl-mechanisms", ownerPath := "amqp:security/section:sasl/type:sasl-mechanisms", index := 1, name := "sasl-server-mechanisms", typeName := "symbol", mandatory := true, defaultValue := none, multiple := true, requires := none, label := "supported sasl mechanisms", artifact := "amqp-core-security-v1.0-os.xml" },
  { owner := "sasl-outcome", ownerPath := "amqp:security/section:sasl/type:sasl-outcome", index := 1, name := "code", typeName := "sasl-code", mandatory := true, defaultValue := none, multiple := false, requires := none, label := "indicates the outcome of the sasl dialog", artifact := "amqp-core-security-v1.0-os.xml" },
  { owner := "sasl-outcome", ownerPath := "amqp:security/section:sasl/type:sasl-outcome", index := 2, name := "additional-data", typeName := "binary", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "additional data as specified in RFC-4422", artifact := "amqp-core-security-v1.0-os.xml" },
  { owner := "sasl-response", ownerPath := "amqp:security/section:sasl/type:sasl-response", index := 1, name := "response", typeName := "binary", mandatory := true, defaultValue := none, multiple := false, requires := none, label := "security response data", artifact := "amqp-core-security-v1.0-os.xml" },
  { owner := "coordinator", ownerPath := "amqp:transactions/section:coordination/type:coordinator", index := 1, name := "capabilities", typeName := "symbol", mandatory := false, defaultValue := none, multiple := true, requires := some "txn-capability", label := "the capabilities supported at the coordinator", artifact := "amqp-core-transactions-v1.0-os.xml" },
  { owner := "declare", ownerPath := "amqp:transactions/section:coordination/type:declare", index := 1, name := "global-id", typeName := "*", mandatory := false, defaultValue := none, multiple := false, requires := some "global-tx-id", label := "global transaction id", artifact := "amqp-core-transactions-v1.0-os.xml" },
  { owner := "declared", ownerPath := "amqp:transactions/section:coordination/type:declared", index := 1, name := "txn-id", typeName := "*", mandatory := true, defaultValue := none, multiple := false, requires := some "txn-id", label := "the allocated transaction id", artifact := "amqp-core-transactions-v1.0-os.xml" },
  { owner := "discharge", ownerPath := "amqp:transactions/section:coordination/type:discharge", index := 1, name := "txn-id", typeName := "*", mandatory := true, defaultValue := none, multiple := false, requires := some "txn-id", label := "identifies the transaction to be discharged", artifact := "amqp-core-transactions-v1.0-os.xml" },
  { owner := "discharge", ownerPath := "amqp:transactions/section:coordination/type:discharge", index := 2, name := "fail", typeName := "boolean", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "indicates the transaction has failed", artifact := "amqp-core-transactions-v1.0-os.xml" },
  { owner := "transactional-state", ownerPath := "amqp:transactions/section:coordination/type:transactional-state", index := 1, name := "txn-id", typeName := "*", mandatory := true, defaultValue := none, multiple := false, requires := some "txn-id", label := "identifies the transaction with which the state is associated", artifact := "amqp-core-transactions-v1.0-os.xml" },
  { owner := "transactional-state", ownerPath := "amqp:transactions/section:coordination/type:transactional-state", index := 2, name := "outcome", typeName := "*", mandatory := false, defaultValue := none, multiple := false, requires := some "outcome", label := "provisional outcome", artifact := "amqp-core-transactions-v1.0-os.xml" },
  { owner := "error", ownerPath := "amqp:transport/section:definitions/type:error", index := 1, name := "condition", typeName := "symbol", mandatory := true, defaultValue := none, multiple := false, requires := some "error-condition", label := "error condition", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "error", ownerPath := "amqp:transport/section:definitions/type:error", index := 2, name := "description", typeName := "string", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "descriptive text about the error condition", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "error", ownerPath := "amqp:transport/section:definitions/type:error", index := 3, name := "info", typeName := "fields", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "map carrying information about the error condition", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "attach", ownerPath := "amqp:transport/section:performatives/type:attach", index := 1, name := "name", typeName := "string", mandatory := true, defaultValue := none, multiple := false, requires := none, label := "the name of the link", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "attach", ownerPath := "amqp:transport/section:performatives/type:attach", index := 2, name := "handle", typeName := "handle", mandatory := true, defaultValue := none, multiple := false, requires := none, label := "the handle for the link while attached", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "attach", ownerPath := "amqp:transport/section:performatives/type:attach", index := 3, name := "role", typeName := "role", mandatory := true, defaultValue := none, multiple := false, requires := none, label := "role of the link endpoint", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "attach", ownerPath := "amqp:transport/section:performatives/type:attach", index := 4, name := "snd-settle-mode", typeName := "sender-settle-mode", mandatory := false, defaultValue := some "mixed", multiple := false, requires := none, label := "settlement policy for the sender", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "attach", ownerPath := "amqp:transport/section:performatives/type:attach", index := 5, name := "rcv-settle-mode", typeName := "receiver-settle-mode", mandatory := false, defaultValue := some "first", multiple := false, requires := none, label := "the settlement policy of the receiver", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "attach", ownerPath := "amqp:transport/section:performatives/type:attach", index := 6, name := "source", typeName := "*", mandatory := false, defaultValue := none, multiple := false, requires := some "source", label := "the source for messages", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "attach", ownerPath := "amqp:transport/section:performatives/type:attach", index := 7, name := "target", typeName := "*", mandatory := false, defaultValue := none, multiple := false, requires := some "target", label := "the target for messages", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "attach", ownerPath := "amqp:transport/section:performatives/type:attach", index := 8, name := "unsettled", typeName := "map", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "unsettled delivery state", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "attach", ownerPath := "amqp:transport/section:performatives/type:attach", index := 9, name := "incomplete-unsettled", typeName := "boolean", mandatory := false, defaultValue := some "false", multiple := false, requires := none, label := "", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "attach", ownerPath := "amqp:transport/section:performatives/type:attach", index := 10, name := "initial-delivery-count", typeName := "sequence-no", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "the sender's initial value for delivery-count", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "attach", ownerPath := "amqp:transport/section:performatives/type:attach", index := 11, name := "max-message-size", typeName := "ulong", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "the maximum message size supported by the link endpoint", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "attach", ownerPath := "amqp:transport/section:performatives/type:attach", index := 12, name := "offered-capabilities", typeName := "symbol", mandatory := false, defaultValue := none, multiple := true, requires := none, label := "the extension capabilities the sender supports", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "attach", ownerPath := "amqp:transport/section:performatives/type:attach", index := 13, name := "desired-capabilities", typeName := "symbol", mandatory := false, defaultValue := none, multiple := true, requires := none, label := "the extension capabilities the sender can use if the receiver supports them", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "attach", ownerPath := "amqp:transport/section:performatives/type:attach", index := 14, name := "properties", typeName := "fields", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "link properties", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "begin", ownerPath := "amqp:transport/section:performatives/type:begin", index := 1, name := "remote-channel", typeName := "ushort", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "the remote channel for this session", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "begin", ownerPath := "amqp:transport/section:performatives/type:begin", index := 2, name := "next-outgoing-id", typeName := "transfer-number", mandatory := true, defaultValue := none, multiple := false, requires := none, label := "the transfer-id of the first transfer id the sender will send", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "begin", ownerPath := "amqp:transport/section:performatives/type:begin", index := 3, name := "incoming-window", typeName := "uint", mandatory := true, defaultValue := none, multiple := false, requires := none, label := "the initial incoming-window of the sender", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "begin", ownerPath := "amqp:transport/section:performatives/type:begin", index := 4, name := "outgoing-window", typeName := "uint", mandatory := true, defaultValue := none, multiple := false, requires := none, label := "the initial outgoing-window of the sender", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "begin", ownerPath := "amqp:transport/section:performatives/type:begin", index := 5, name := "handle-max", typeName := "handle", mandatory := false, defaultValue := some "4294967295", multiple := false, requires := none, label := "the maximum handle value that can be used on the session", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "begin", ownerPath := "amqp:transport/section:performatives/type:begin", index := 6, name := "offered-capabilities", typeName := "symbol", mandatory := false, defaultValue := none, multiple := true, requires := none, label := "the extension capabilities the sender supports", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "begin", ownerPath := "amqp:transport/section:performatives/type:begin", index := 7, name := "desired-capabilities", typeName := "symbol", mandatory := false, defaultValue := none, multiple := true, requires := none, label := "the extension capabilities the sender can use if the receiver supports them", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "begin", ownerPath := "amqp:transport/section:performatives/type:begin", index := 8, name := "properties", typeName := "fields", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "session properties", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "close", ownerPath := "amqp:transport/section:performatives/type:close", index := 1, name := "error", typeName := "error", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "error causing the close", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "detach", ownerPath := "amqp:transport/section:performatives/type:detach", index := 1, name := "handle", typeName := "handle", mandatory := true, defaultValue := none, multiple := false, requires := none, label := "the local handle of the link to be detached", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "detach", ownerPath := "amqp:transport/section:performatives/type:detach", index := 2, name := "closed", typeName := "boolean", mandatory := false, defaultValue := some "false", multiple := false, requires := none, label := "if true then the sender has closed the link", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "detach", ownerPath := "amqp:transport/section:performatives/type:detach", index := 3, name := "error", typeName := "error", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "error causing the detach", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "disposition", ownerPath := "amqp:transport/section:performatives/type:disposition", index := 1, name := "role", typeName := "role", mandatory := true, defaultValue := none, multiple := false, requires := none, label := "directionality of disposition", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "disposition", ownerPath := "amqp:transport/section:performatives/type:disposition", index := 2, name := "first", typeName := "delivery-number", mandatory := true, defaultValue := none, multiple := false, requires := none, label := "lower bound of deliveries", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "disposition", ownerPath := "amqp:transport/section:performatives/type:disposition", index := 3, name := "last", typeName := "delivery-number", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "upper bound of deliveries", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "disposition", ownerPath := "amqp:transport/section:performatives/type:disposition", index := 4, name := "settled", typeName := "boolean", mandatory := false, defaultValue := some "false", multiple := false, requires := none, label := "indicates deliveries are settled", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "disposition", ownerPath := "amqp:transport/section:performatives/type:disposition", index := 5, name := "state", typeName := "*", mandatory := false, defaultValue := none, multiple := false, requires := some "delivery-state", label := "indicates state of deliveries", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "disposition", ownerPath := "amqp:transport/section:performatives/type:disposition", index := 6, name := "batchable", typeName := "boolean", mandatory := false, defaultValue := some "false", multiple := false, requires := none, label := "batchable hint", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "end", ownerPath := "amqp:transport/section:performatives/type:end", index := 1, name := "error", typeName := "error", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "error causing the end", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "flow", ownerPath := "amqp:transport/section:performatives/type:flow", index := 1, name := "next-incoming-id", typeName := "transfer-number", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "flow", ownerPath := "amqp:transport/section:performatives/type:flow", index := 2, name := "incoming-window", typeName := "uint", mandatory := true, defaultValue := none, multiple := false, requires := none, label := "", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "flow", ownerPath := "amqp:transport/section:performatives/type:flow", index := 3, name := "next-outgoing-id", typeName := "transfer-number", mandatory := true, defaultValue := none, multiple := false, requires := none, label := "", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "flow", ownerPath := "amqp:transport/section:performatives/type:flow", index := 4, name := "outgoing-window", typeName := "uint", mandatory := true, defaultValue := none, multiple := false, requires := none, label := "", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "flow", ownerPath := "amqp:transport/section:performatives/type:flow", index := 5, name := "handle", typeName := "handle", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "flow", ownerPath := "amqp:transport/section:performatives/type:flow", index := 6, name := "delivery-count", typeName := "sequence-no", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "the endpoint's value for the delivery-count sequence number", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "flow", ownerPath := "amqp:transport/section:performatives/type:flow", index := 7, name := "link-credit", typeName := "uint", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "the current maximum number of messages that can be received", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "flow", ownerPath := "amqp:transport/section:performatives/type:flow", index := 8, name := "available", typeName := "uint", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "the number of available messages", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "flow", ownerPath := "amqp:transport/section:performatives/type:flow", index := 9, name := "drain", typeName := "boolean", mandatory := false, defaultValue := some "false", multiple := false, requires := none, label := "indicates drain mode", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "flow", ownerPath := "amqp:transport/section:performatives/type:flow", index := 10, name := "echo", typeName := "boolean", mandatory := false, defaultValue := some "false", multiple := false, requires := none, label := "request state from partner", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "flow", ownerPath := "amqp:transport/section:performatives/type:flow", index := 11, name := "properties", typeName := "fields", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "link state properties", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "open", ownerPath := "amqp:transport/section:performatives/type:open", index := 1, name := "container-id", typeName := "string", mandatory := true, defaultValue := none, multiple := false, requires := none, label := "the id of the source container", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "open", ownerPath := "amqp:transport/section:performatives/type:open", index := 2, name := "hostname", typeName := "string", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "the name of the target host", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "open", ownerPath := "amqp:transport/section:performatives/type:open", index := 3, name := "max-frame-size", typeName := "uint", mandatory := false, defaultValue := some "4294967295", multiple := false, requires := none, label := "proposed maximum frame size", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "open", ownerPath := "amqp:transport/section:performatives/type:open", index := 4, name := "channel-max", typeName := "ushort", mandatory := false, defaultValue := some "65535", multiple := false, requires := none, label := "the maximum channel number that can be used on the connection", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "open", ownerPath := "amqp:transport/section:performatives/type:open", index := 5, name := "idle-time-out", typeName := "milliseconds", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "idle time-out", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "open", ownerPath := "amqp:transport/section:performatives/type:open", index := 6, name := "outgoing-locales", typeName := "ietf-language-tag", mandatory := false, defaultValue := none, multiple := true, requires := none, label := "locales available for outgoing text", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "open", ownerPath := "amqp:transport/section:performatives/type:open", index := 7, name := "incoming-locales", typeName := "ietf-language-tag", mandatory := false, defaultValue := none, multiple := true, requires := none, label := "desired locales for incoming text in decreasing level of preference", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "open", ownerPath := "amqp:transport/section:performatives/type:open", index := 8, name := "offered-capabilities", typeName := "symbol", mandatory := false, defaultValue := none, multiple := true, requires := none, label := "extension capabilities the sender supports", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "open", ownerPath := "amqp:transport/section:performatives/type:open", index := 9, name := "desired-capabilities", typeName := "symbol", mandatory := false, defaultValue := none, multiple := true, requires := none, label := "extension capabilities the sender can use if the receiver supports them", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "open", ownerPath := "amqp:transport/section:performatives/type:open", index := 10, name := "properties", typeName := "fields", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "connection properties", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "transfer", ownerPath := "amqp:transport/section:performatives/type:transfer", index := 1, name := "handle", typeName := "handle", mandatory := true, defaultValue := none, multiple := false, requires := none, label := "", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "transfer", ownerPath := "amqp:transport/section:performatives/type:transfer", index := 2, name := "delivery-id", typeName := "delivery-number", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "alias for delivery-tag", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "transfer", ownerPath := "amqp:transport/section:performatives/type:transfer", index := 3, name := "delivery-tag", typeName := "delivery-tag", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "transfer", ownerPath := "amqp:transport/section:performatives/type:transfer", index := 4, name := "message-format", typeName := "message-format", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "indicates the message format", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "transfer", ownerPath := "amqp:transport/section:performatives/type:transfer", index := 5, name := "settled", typeName := "boolean", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "transfer", ownerPath := "amqp:transport/section:performatives/type:transfer", index := 6, name := "more", typeName := "boolean", mandatory := false, defaultValue := some "false", multiple := false, requires := none, label := "indicates that the message has more content", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "transfer", ownerPath := "amqp:transport/section:performatives/type:transfer", index := 7, name := "rcv-settle-mode", typeName := "receiver-settle-mode", mandatory := false, defaultValue := none, multiple := false, requires := none, label := "", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "transfer", ownerPath := "amqp:transport/section:performatives/type:transfer", index := 8, name := "state", typeName := "*", mandatory := false, defaultValue := none, multiple := false, requires := some "delivery-state", label := "the state of the delivery at the sender", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "transfer", ownerPath := "amqp:transport/section:performatives/type:transfer", index := 9, name := "resume", typeName := "boolean", mandatory := false, defaultValue := some "false", multiple := false, requires := none, label := "indicates a resumed delivery", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "transfer", ownerPath := "amqp:transport/section:performatives/type:transfer", index := 10, name := "aborted", typeName := "boolean", mandatory := false, defaultValue := some "false", multiple := false, requires := none, label := "indicates that the message is aborted", artifact := "amqp-core-transport-v1.0-os.xml" },
  { owner := "transfer", ownerPath := "amqp:transport/section:performatives/type:transfer", index := 11, name := "batchable", typeName := "boolean", mandatory := false, defaultValue := some "false", multiple := false, requires := none, label := "batchable hint", artifact := "amqp-core-transport-v1.0-os.xml" },
]

/-- The fields of one type, in the order the artifact declares them. -/
def fieldsOf (ownerPath : String) : List FieldDecl :=
  fields.filter (fun entry => entry.ownerPath == ownerPath)

/-- The mandatory fields of one type: the prefix a decoder must see, and the
reason trailing absent fields can be omitted from an encoding. -/
def mandatoryFields (ownerPath : String) : List FieldDecl :=
  (fieldsOf ownerPath).filter (fun entry => entry.mandatory)

end SpecAMQP.Generated.Oasis
